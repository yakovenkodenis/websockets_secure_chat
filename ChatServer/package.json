{
  "name": "chat_server",
  "version": "1.0.0",
  "description": "The WebSockets chat server",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "babel-node": "babel-node --presets es2015,stage-0 --compact=false --ignore='foo|bar|baz'",
    "watch": "nodemon --exec npm run babel-node -- server.js"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com:yakovenkodenis/websockets_secure_chat.git"
  },
  "keywords": [
    "websockets",
    "chat",
    "encryption"
  ],
  "author": "Denis Yakovenko",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.6.5",
    "babel-core": "^6.7.4",
    "babel-preset-es2015": "^6.6.0",
    "babel-preset-stage-0": "^6.5.0"
  },
  "dependencies": {
    "decimal.js": "^5.0.8",
    "secure-random": "^1.1.1",
    "socket.io": "^1.4.5"
  }
}
